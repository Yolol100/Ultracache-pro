UltraCache Pro 11.0.33
- Delay-JS safe-exclusion lijst uitgebreid met gangbare payment SDKs: Braintree, Razorpay, Square, Paddle, Authorize.Net.
- Delay-JS safe-exclusion lijst uitgebreid met consent managers: CookieYes/Cookie Law Info, Usercentrics, iubenda.
- Pure data/allowlist-wijziging in compat/delay-js-exclusions.json (13 entries, idempotent additief samengevoegd).
- Effect strikt conservatief: deze scripts worden niet langer vertraagd; kan nooit méér scripts vertragen. Inert op sites die ze niet laden.
- Autopilot/PageSpeed-Auto pad ongewijzigd (eigen hardcoded lijst). Geen PHP-logica, hooks, REST-namespace of option keys gewijzigd.
- Versie consistent bijgewerkt: header, UCP_VERSION, readme stable tag, RELEASE-NOTES.

UltraCache Pro 11.0.32
- Echte kleinere productievarianten gemaakt voor React admin CSS/JS assets, zodat `.min` bestanden niet langer identiek zijn aan de debugbestanden.
- Ontbrekende 11.0.30 releasehistorie toegevoegd voor betere traceability.
- Versie- en vertaalmetadata gelijkgetrokken naar 11.0.32.
- Geen runtime- of businesslogica gewijzigd; deze build is gericht op release-hygiëne.

UltraCache Pro 11.0.31
- React admin tab-deeplinking gerepareerd voor bestaande admin-links, redirects en tab-queryparameters.
- Bulk-action JavaScript runtime fix toegevoegd: veilige sprintf-fallback.
- Admin-router accepteert dashboard als compatibiliteitsalias voor overzicht.
- POT/release metadata gelijkgetrokken.
- Geen risicovolle cleanup uitgevoerd; dynamisch gebruikte WordPress-onderdelen behouden.

UltraCache Pro 11.0.30
- Added admin-only diagnostics quality-summary endpoint for release and support checks.
- Extended support reports and log packages with quality summary and runtime-test snapshots.
- Added WP-CLI diagnostics and runtime-test commands with JSON output support.
- Added Site Health runtime-test visibility and competitive feature matrix documentation.

UltraCache Pro 11.0.29
- Hardened sensitive admin downloads with nosniff, noindex and no-store response headers.
- Reduced public CWV beacon token window from weekly to daily buckets while keeping the previous bucket for cached frontend HTML.
- Release package hygiene: development Composer/Scoper files are excluded from the fixed distribution zip.

## 11.0.26 - Bugfix privacy URL and user-id log hardening
- Redacts user identifiers in support-package JSONL log rows and context payloads.
- Redacts sensitive WooCommerce/account/payment URL path markers in helper and support-package log URL redaction.
- Keeps cache, CSS/LCP, preload, Asset Manager and admin UI behavior unchanged.

## 11.0.25 - Bugfix privacy log hardening

- Redacts plain-text helper log messages before writing events.log.
- Redacts REST log query rows before returning them to the admin UI, including legacy rows stored before this build.
- Redacts logger request URLs before database storage by stripping query strings.
- No feature behavior changes; staging runtime testing remains required for browser, WooCommerce and payment flows.

## 11.0.24 - Runtime acceptance privacy hardening

- Hardened diagnostics/log redaction for WooCommerce/order, customer, payment, cart and session-related context keys.
- Added release-candidate QA notes for static validation and staging runtime gates.

## 11.0.23 - WordPress-native admin UI polish

- Polished the React admin toward native WordPress/Gutenberg visual rhythm without changing cache or optimization behavior.
- Added compact native admin header status, accessible tab semantics, a DataViews-like PageSpeed Readiness summary and clearer Asset Manager microcopy.
- Improved scoped admin CSS for cards, badges, focus states, loading states, readiness details, asset rows and responsive wp-admin behavior.
- Kept REST, nonce, capability and admin asset scope protections intact.

## 11.0.22 - Ultimate polish, release hardening and fail-closed guards

- Added CSS profile expiry metadata, renderer status and protected-wins enforcement so protected CSS cannot also be treated as delayed/removable.
- Added optional LCP CDN host allowlist support while keeping measured page URLs same-origin and text LCP excluded from automatic preload/fetchpriority.
- Asset Manager now skips all unload rules on WooCommerce/account/payment-sensitive requests unless an explicit manual override is enabled.
- Improved preload crawler skip/status reasons for sensitive URLs, dynamic nonce/query URLs, redirects and unsupported content types.
- Expanded Conflict Guard Cloudflare Super Page Cache detection and duplicate HTML rewrite guidance.
- Expanded PageSpeed Readiness diagnostics with advanced-cache, writability, font preload, WooCommerce safety and REST/admin security status.
- Added PageSpeed Auto v12 migration with extra Elementor/WooCommerce/forms/menu/accessibility safelists and safe defaults only.

## 11.0.21 - Release polish and safety hardening

- Hardened CSS profile storage: external renderer rows are sanitized, bounded and re-routed away from safe-removal on sensitive WooCommerce/account/payment URLs.
- Raised PageSpeed Auto LCP confidence defaults from 80 to 85 for automatic preload/fetchpriority use.
- Added LCP resource safety checks so automatic profile-based preloads only use same-origin image-like resources, not arbitrary local pages.
- Added per-URL/device LCP stale rollback helper for bad measured profiles or layout changes.
- Cleaned Conflict Guard plugin detection and added Cloudflare / Cloudflare Super Page Cache plugin-level edge-cache overlap reporting.
- Added Asset Manager rule aliases for unload/protect, URL exceptions, post type, device and logged-in/logged-out scopes without changing existing rule syntax.
- Hardened preload status metadata sanitization to avoid noisy nested-value casts in diagnostics.
- Added PageSpeed Auto v11 safelist polish migration for Elementor responsive states, WooCommerce blocks/notices, menu states, consent/CAPTCHA widgets and common slider/modal states.


## 11.0.20 - CSS/LCP profiles, Preload v2 and PageSpeed readiness

- Added conservative per-URL CSS profile infrastructure with explicit buckets for critical CSS, delayed CSS, safely removable CSS and protected CSS.
- Added dynamic CSS safelists for Elementor, WooCommerce, menus, popups, sliders, forms, sticky headers, responsive hidden states, consent/cookie banners and CAPTCHA assets.
- Added high-confidence LCP profiles per URL and device, including resource URL, srcset/sizes, selector hint, confidence score, type and freshness status.
- Automatic LCP preload/fetchpriority now uses only fresh, high-confidence profiles; stale profiles fail closed and are marked for regeneration.
- Added Preload Crawler v2 priorities for homepage, recently purged URLs, menu URLs, recently modified content and sitemap URLs.
- Added preload URL statuses: pending, processing, cached, skipped and failed, including skip reasons and throttling/high-load pause support.
- Expanded Asset Manager rules with URL/path exception scopes, post type/device scopes and logged-out rules, while protected assets remain blocked.
- Expanded Conflict Guard with feature-level overlap reporting for page cache, Critical CSS, Delay JS, lazyload, font optimization, CDN/edge cache and asset unload.
- Added PageSpeed Readiness diagnostics for cache, LCP, CSS, JS delay, resource hints, asset unload test mode, preload and conflicts.
- Added PageSpeed Auto v10 migration for safe defaults without forcing aggressive CSS/JS rewrites.

## 11.0.18 - Asset Intelligence and conflict guard
- Adds Asset Intelligence to the Asset Manager snapshot: owner grouping, risk levels, reasons and safe unload recommendations.
- Adds per-asset suggested scopes so admins can start with URL-scoped test-mode rules instead of broad global unloads.
- Adds a conflict guard for Delay JS when another optimization plugin is already active, with diagnostics logging instead of silently stacking duplicate rewrite layers.

## 11.0.17 - PageSpeed LCP hint refinement

- Added real-user background-image LCP detection so Elementor/cover hero backgrounds can be selected and preloaded from measured hints.
- Preserved measured responsive image metadata (`srcset`/`sizes`) from browser scans and RUM beacons for more accurate LCP preload links.
- Improved PageSpeed Auto migration so measured background/image priority can work without enabling risky CSS/JS rewrites.

11.0.16 (Asset Manager execution upgrade)
- Added an admin Asset Manager panel that reads the latest frontend asset snapshot and can create safe unload/keep rules from real enqueued CSS and JS handles. This brings the workflow closer to Perfmatters/Asset CleanUp while keeping UltraCache's staging-first defaults.
- Asset snapshots now include owner/source grouping and protected-asset markers, making it easier to identify plugin, theme, core, external and inline assets before disabling anything.
- Asset unloads now fail closed for critical core, WooCommerce, payment, form, CAPTCHA, cookie/consent and builder/runtime assets. These protected handles are skipped and logged instead of being deregistered by broad rules.
- Added a REST diagnostics endpoint for the latest Asset Manager snapshot so the React admin can show actionable frontend asset data without exposing it publicly.
- Added helper rule examples for disable-everywhere, disable-on-current-URL and keep-on-current-URL workflows, with Asset Test Mode kept visible next to the rule editor.

11.0.15 (PageSpeed execution upgrade)
- Preload now prioritizes recently modified public content before sitemap/archive URLs and PageSpeed Auto switches from light Range requests to full GET warming, so purged/updated pages are more likely to build a real HTML cache file before visitors arrive.
- Per-URL browser speed scans are now stored in a bounded URL/device map. LCP, render-blocking CSS and Delay JS hints no longer depend only on the single latest scan, so homepage, landing pages and product pages can each keep their own measured hints.
- The React diagnostics scan now accepts a target URL instead of only scanning the homepage. This makes it usable for key templates such as product, category and high-value landing pages.
- Delay JS can preload delayed script files before interaction, preserving lower main-thread work while reducing latency when delayed scripts are eventually released.
- A purged single URL is re-queued for preload when preload queueing is enabled, so targeted purges recover cache warmth faster.



11.0.14 (review fix: dead option flags removed)
- Removed three option flags that were saved as settings but never read by any code, so they could never take effect: enable_adaptive_speculation (was default ON), enable_css_stale_refresh (was default ON) and enable_logged_in_private_cache (was default OFF). They were leftover scaffolding with no UI field, no logic and no preset reference; deleting them from the defaults and the sanitizer removes a false "this is active" impression. No working feature was affected: speculative loading still runs via enable_speculative_loading, CSS artifacts via the css_artifact_* / enable_css_queue options, and object caching via the existing object-cache options.

11.0.14 (key single-source-of-truth + self-test)
- Single source of truth for mobile detection: the mobile user-agent regex now lives in one place (UCP_Helpers::mobile_user_agent_regex) and is written into dropin-config.php, so advanced-cache.php and the PHP fallback can no longer drift apart on the guest vs guest-mobile suffix.
- Quality suite: the runtime cache test now includes an automated cache-key consistency check that recomputes the key the drop-in way and the PHP way for several tricky URLs (including the /foo/bar vs /foo-bar collision pair) and fails if they ever diverge.

11.0.13 (object-cache UI + design fixes)
- New server-rendered "Object Cache" admin sub-page (works without a React rebuild): shows live status (external object cache active, current drop-in owner, phpredis/APCu availability, live Redis connectivity) and provides install / uninstall actions for the UltraCache Redis and APCu drop-ins. All actions are capability- + nonce-protected and only ever remove UltraCache's own drop-in, never a third-party one. Includes a copy-paste wp-config.php Redis snippet.
- Object-cache install handlers now redirect back to the Object Cache page and surface a flash notice there.
- Design fix: the compiled React admin stylesheet referenced ~21 design tokens (short aliases like --ucp-primary/--ucp-muted/--ucp-border/--ucp-text and the whole --ucp-premium-* family, plus --ucp-grid-columns) that were never defined, so those declarations silently broke (missing muted text colour, broken focus rings, unstyled "premium" cards). Added the missing aliases to both ucp-admin-tokens.css and ucp-admin-tokens.min.css so every var() reference resolves to the canonical token.

11.0.12 (auto drop-in + Redis object cache)
- Auto drop-in: advanced-cache.php and the WP_CACHE constant are now installed automatically on activation (allow_dropin_writes / allow_wp_config_write default ON). The installer still backs up and preserves any third-party drop-in, only ever takes over its own, and degrades gracefully with an admin notice when wp-content / wp-config are not writable. The previous migration no longer force-disables these flags.
- Redis object cache: added a full phpredis-backed object-cache drop-in (dropins/object-cache-redis.php) implementing the complete WordPress object-cache API (get/set/add/replace/delete, *_multiple, incr/decr via atomic INCRBY/DECRBY, flush, flush_group, flush_runtime, global + non-persistent groups, multisite key segmentation, installation salt). Connection via WP_REDIS_HOST/PORT/PASSWORD/DATABASE/TIMEOUT/PREFIX constants. Degrades to a non-persistent runtime cache when Redis is unavailable, and never flushes beyond this installation's namespace.
- New enable_redis_object_cache option + admin-post installer (cap + nonce + connectivity probe), object-cache status now reports backend + live Redis connectivity, REST status exposes objectCacheDetail, and clean uninstall removes our Redis drop-in as well.

11.0.11 (review fixes)
- Cache hit-rate: unknown request/response cookies are no longer blocked by default (was the main cause of low real-world hit-rates). Strict mode stays available via the ucp_block_unknown_request_cookies / ucp_dropin_block_unknown_cookies filters. Safe-cookie lists expanded with common analytics, consent, heatmap, LinkedIn, Stripe and Cloudflare cookies, in the drop-in, the dropin-config writer and the PHP request policy.
- Cache key: added a short hash of the full request path to both advanced-cache.php and the PHP key builder so '/foo/bar' and '/foo-bar' can never resolve to the same cache file.
- Mobile cache: the PHP fallback now uses the exact same user-agent signature as advanced-cache.php, so the early drop-in and the PHP path compute an identical key (fixes systematic fast-path misses on mobile).
- WebP/AVIF: generated variants are now actually delivered via cache-safe <picture> rewriting in the front-end buffer (works inside cached HTML, no Vary: Accept needed). Output buffering now also starts when only image variant generation is enabled.
- Purge: per-URL purge and purge-all now also remove the .gz and .br pre-compressed variants so a stale compressed copy can never be served after a purge.
- HTML minify: now defaults to OFF (with comment removal), and the collapse no longer removes whitespace between adjacent tags, so inline elements can no longer be glued together. Enable on staging when needed.
11.0.10
- Hardened CWV counter ordering and fail-closed transient writes.
- Added runtime-backed APCu object-cache fallback and wp_cache_reset compatibility.
- Sanitized cloud API header material before outbound requests.

UltraCache Pro 11.0.10

- Hardened APCu object-cache increment/decrement paths with atomic APCu operations.
- Added validated job-table helpers for queue SQL and guarded empty table states.
- Reformatted remaining compact callbacks and lazy-render selector assembly for stricter WPCS readability.

UltraCache Pro 11.0.8

- Hardened CWV/RUM burst limits so per-IP and site-wide caps are global per minute instead of per metric.
- Added locked counter updates for all CWV/RUM minute, visitor and daily rate-limit counters to reduce concurrent bypass risk.
- Added strict scheme, host and port checks for same-origin CWV/RUM headers and LCP hint URLs.
- Added length bounds before decoding LCP element JSON metadata.
- Switched APCu object-cache drop-in installation to WordPress filesystem writes.
- Removed UltraCache-owned object-cache.php during clean uninstall.
- Removed duplicate REST action route registrations from the quality suite layer.
- Added HTTP response-size limits for remote CSS/font/cloud/Cloudflare requests.
- Reformatted compact admin/font/router code paths for stricter WPCS readability.

UltraCache Pro 11.0.3

- Fixed cache toast CSS keyframes in both readable and production assets so admin cache notifications animate and dismiss consistently.
- Re-ran full static validation after the UI/release fixes.

UltraCache Pro 11.0.2

- Fixed production minified React asset so translated UI copy keeps spaces inside strings.
- Corrected malformed fallback admin navigation selectors.
- Re-synced minified assets safely from validated readable sources.

UltraCache Pro 11.0.1 release package

This production zip excludes tests and internal docs/dev notes. Runtime assets, compatibility lists, language template, drop-in templates and plugin files are included.

Validation before production:
- Run in a clean WordPress staging install.
- Run Plugin Check or wp plugin check ultracache-pro --format=json.
- Test activation, deactivation, uninstall, cache purge, preload and WooCommerce cart/checkout/account/order-pay flows.
